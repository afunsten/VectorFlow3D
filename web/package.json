{
  "name": "vectorflow3d-observer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VectorFlow3D Phase 5.5 — browser WebGPU observer client for the vf.bridge.v1 WebSocket stream (AABB proxy boxes; camera/pick/pin).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "node test/smoke.mjs",
    "smoke:mesh": "node test/mesh.mjs"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
